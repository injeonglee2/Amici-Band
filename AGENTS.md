# Amici 개발 규칙

UI 변경 전 `docs/UI-CONSISTENCY.md`의 공용 양식 목록과 판단 흐름을 따른다.

- 먼저 `src/components`와 유사 화면을 검색하고 같은 역할의 공용 컴포넌트를 재사용한다.
- 시각적 차이만으로 새 컴포넌트나 새 CSS를 만들지 않는다. 기능적 차이가 있으면 기존 공용 컴포넌트에 이름 있는 variant를 추가한다.
- 같은 패턴이 두 곳 이상이면 공용 컴포넌트로 추출하고 양식 목록을 갱신한다.
- 모바일 폭, 키보드·스크린리더 상태, 빌드를 확인한다.
