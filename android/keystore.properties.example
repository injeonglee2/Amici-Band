# Copy this file to keystore.properties and fill in the values locally.
# Never commit keystore.properties or the keystore itself.
storeFile=C:/secure/path/signing.keystore
storePassword=change-me
keyAlias=change-me
keyPassword=change-me
